{
  "name": "edgetunnel",
  "version": "2.1.0",
  "private": true,
  "description": "edgetunnel - Cloudflare Workers/Pages edge tunnel",
  "main": "_worker.js",
  "type": "module",
  "scripts": {
    "check": "node --check _worker.js",
    "dev": "wrangler dev --config wrangler.dev.toml --local --port 8787",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^4.86.0"
  }
}
