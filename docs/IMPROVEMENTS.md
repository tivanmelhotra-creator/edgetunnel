# نقاط قابل بهبود (TODO)

فهرست فرصت‌های بهبود شناسایی‌شده هنگام مستندسازی کد. اولویت‌بندی برای استپ‌های بعدی.

## امنیت / احراز هویت (استپ ۳)
- [ ] **مقایسه توکن غیرثابت‌زمان:** مقایسه‌های `===` روی cookie/token مستعد timing leak هستند. می‌توان از `constantTimeEqual` (که برای TLS موجود است) استفاده کرد.
- [ ] **اعتبارسنجی ورودی POST:** برخی endpointهای `admin/*` فقط فیلدهای پایه را چک می‌کنند؛ افزودن اعتبارسنجی نوع/طول.
- [ ] **پیام خطای یکدست:** خطاها گاهی متن خام و گاهی JSON برمی‌گردانند — یکدست‌سازی فرمت `{error, msg}`.
- [ ] **سرریز/طول ورودی:** محدودسازی اندازه body در `request.json()`/`request.text()` در ادمین.

## سیستم اشتراک `/sub` (استپ ۴)
- [ ] **DoH fallback:** بررسی رفتار وقتی DoH اصلی تایم‌اوت می‌کند.
- [ ] **رِگِکس فرمت IP:** نودهای نامعتبر `console.warn` و skip می‌شوند؛ بررسی موارد لبه‌ای IPv6/پورت.
- [ ] **سازگاری UA:** ماتریس تست برای Clash/Meta/Sing-box/Surge/Loon/QuanX.

## مسیر داغ ترنسپورت (استپ ۵)
- [ ] **Backpressure صف بالادست:** بازبینی سقف 16MB/4096 و آزادسازی صحیح در پاک‌سازی.
- [ ] **بستن سوکت:** اطمینان از فراخوانی `closeSocketQuietly` در همه مسیرهای خطا.
- [ ] **نشت حافظه:** بررسی نگه‌داشتن reference به reader/writer پس از قطع اتصال.
- [ ] **پارامترهای قابل‌تنظیم:** ثابت‌های buffer/concurrency به‌صورت env قابل تنظیم شوند؟

## CI/CD و نگه‌داری (استپ ۶)
- [ ] **lint/check workflow:** افزودن `node --check` در CI (نیاز به permission workflows → احتمال ویرایش دستی).
- [ ] **همگام‌سازی upstream:** workflow `sync.yml` فقط روی fork فعال است؛ مستندسازی.
- [ ] **به‌روزرسانی README:** افزودن بخش توسعه محلی (`npm run dev`).

## عمومی
- [ ] **پایداری استقرار:** فایل `wrangler.toml` تولید دست‌نخورده بماند؛ توسعه با `wrangler.dev.toml`.
- [ ] **بدون شکستن سازگاری:** هر تغییر باید با کلاینت‌های موجود و upstream سازگار بماند.
